# 🔑 Настройка API

## Обзор требований

| API | Статус | Что нужно | Стоимость |
|-----|--------|-----------|-----------|
| **OpenAI** | ✅ Обязательно | API ключ | ~$0.003-0.01/запрос |
| **OpenAlex** | 📧 Email | Вежливость | Бесплатно |
| **Crossref** | 📧 Email | Вежливость | Бесплатно |
| **PubMed** | 📧 Email | Обязательно (NCBI) | Бесплатно |
| **ArXiv** | ✅ Готов | Не требуется | Бесплатно |
| **Semantic Scholar** | ✅ Готов | Не требуется | Бесплатно |

---

## 1. OpenAI API (ОБЯЗАТЕЛЬНО)

### Получение ключа

1. Перейдите на https://platform.openai.com/api-keys
2. Войдите или создайте аккаунт
3. Нажмите "Create new secret key"
4. Скопируйте ключ (вы увидите его только один раз!)

### Настройка

Создайте файл `.env` в корне проекта:

```bash
OPENAI_API_KEY=sk-ваш_ключ_здесь
```

**Пример `.env` файла:**
```
OPENAI_API_KEY=sk-proj-abcd1234efgh5678ijkl9012mnop3456
```

### Проверка

```bash
# Проверьте, что файл создан
cat .env | grep OPENAI_API_KEY

# Убедитесь, что .env в .gitignore
grep ".env" .gitignore
```

### Стоимость

- **Модель по умолчанию:** gpt-4o-mini
- **Стоимость:** ~$0.15 за 1M входных токенов
- **Средний запрос:** ~20K токенов
- **Примерная стоимость:** $0.003-0.01 за один полный поиск

---

## 2. Настройка Email (рекомендуется)

Некоторые API требуют указания email для вежливого использования (polite pool).

### Настройка в config.py

Откройте `config.py` и измените:

```python
API_CONTACT = {
    "email": "ваш.email@example.com",  # ← Замените на ваш email
    "user_agent": "AI-Research-Agent/1.0",
}
```

**Где используется:**
- OpenAlex — для вежливого пула (быстрее обработка)
- Crossref — рекомендация API
- PubMed — обязательное требование NCBI

### Зачем нужен email?

- 📧 Получение уведомлений о проблемах API
- ⚡ Доступ к "polite pool" (приоритетная обработка)
- ✅ Соответствие правилам использования API

---

## 3. Опциональные настройки

### GitHub API (опционально)

Для поиска GitHub репозиториев (опциональная функция):

1. Создайте Personal Access Token: https://github.com/settings/tokens
2. Добавьте в `.env`:
   ```
   GITHUB_TOKEN=ghp_ваш_токен
   ```

**Примечание:** Работает и без токена, но с ограничениями rate limit.

### Включение/выключение источников

В `config.py`:

```python
SOURCES_ENABLED = {
    "openalex": True,
    "semantic_scholar": True,
    "crossref": True,
    "arxiv": True,
    "pubmed": True,  # Отключите, если не нужна биомедицина
}
```

---

## 4. Проверка настройки

### Автоматическая проверка

```bash
python tests/test_basic.py
```

**Ожидаемый результат:**
```
🧪 БАЗОВОЕ ТЕСТИРОВАНИЕ AI АГЕНТА
✅ OPENAI_API_KEY найден (длина: 51)
✅ Конфигурация загружена
✅ LLM работает
✅ Агент создан успешно

✅ Пройдено: 3/3
🎉 Все тесты пройдены! Агент готов к работе.
```

### Ручная проверка

**Проверка .env:**
```bash
cat .env
```

**Проверка config.py:**
```bash
grep -A 2 "API_CONTACT" config.py
```

---

## 5. Rate Limits

Все ограничения API уже учтены в коде через задержки:

| API | Rate Limit | Задержка в коде |
|-----|------------|-----------------|
| ArXiv | 1 req/3 sec | 3.0 сек |
| PubMed | 3 req/sec | 0.33 сек |
| OpenAlex | Вежливый пул | 0.1 сек |
| Semantic Scholar | 100 req/5 min | 0.1 сек |
| Crossref | 50 req/sec | 0.1 сек |

**Конфигурация** (`config.py`):
```python
RATE_LIMITS = {
    "arxiv_delay": 3.0,
    "pubmed_delay": 0.33,
    "openalex_delay": 0.1,
    "semantic_scholar_delay": 0.1,
    "crossref_delay": 0.1,
}
```

---

## 6. Troubleshooting

### ❌ "OPENAI_API_KEY not found"

**Причины:**
- Файл `.env` не создан
- Неправильное имя переменной
- Файл .env в неправильной директории

**Решение:**
```bash
# Убедитесь, что вы в корне проекта
cd /path/to/ai_agent

# Создайте .env файл
echo "OPENAI_API_KEY=sk-ваш_ключ" > .env

# Проверьте
cat .env
```

### ❌ "Rate limit exceeded" (OpenAI)

**Причины:**
- Исчерпан лимит запросов
- Недостаточно средств на аккаунте

**Решение:**
1. Проверьте баланс: https://platform.openai.com/usage
2. Подождите несколько минут
3. Увеличьте лимит в настройках аккаунта

### ❌ "API request failed" (OpenAlex, Crossref, etc.)

**Причины:**
- API временно недоступен
- Проблемы с сетью
- Не указан email

**Решение:**
1. Проверьте интернет-соединение
2. Укажите email в `config.py`
3. Попробуйте позже
4. Отключите проблемный источник:
   ```python
   SOURCES_ENABLED["openalex"] = False
   ```

### ❌ Медленная работа

**Решение:**
- Укажите email для OpenAlex/Crossref (polite pool)
- Уменьшите `max_papers` в запросе
- Отключите ненужные источники

---

## 7. Безопасность

### Защита API ключей

✅ **DO:**
- Храните ключи в `.env`
- Добавьте `.env` в `.gitignore`
- Используйте разные ключи для разработки и production
- Регулярно ротируйте ключи

❌ **DON'T:**
- Не коммитьте `.env` в git
- Не храните ключи в коде
- Не передавайте ключи в открытом виде
- Не используйте один ключ везде

### .gitignore

Убедитесь, что файл `.gitignore` содержит:
```
.env
.env.local
.env.*.local
```

---

## 8. Production настройки

Для production рекомендуется использовать переменные окружения:

```bash
# Linux/macOS
export OPENAI_API_KEY="sk-..."
export GITHUB_TOKEN="ghp_..."

# Windows
set OPENAI_API_KEY=sk-...
set GITHUB_TOKEN=ghp_...
```

Или использовать систему управления секретами (AWS Secrets Manager, Azure Key Vault, etc.)

---

## 9. Полезные ссылки

- **OpenAI API:** https://platform.openai.com/docs
- **OpenAlex API:** https://docs.openalex.org/
- **Semantic Scholar API:** https://api.semanticscholar.org/
- **Crossref API:** https://www.crossref.org/documentation/
- **ArXiv API:** https://arxiv.org/help/api
- **PubMed API:** https://www.ncbi.nlm.nih.gov/books/NBK25497/

---

**Готово!** После настройки всех API агент будет полностью функционален. 🚀

Для быстрого старта см. [GETTING_STARTED.md](GETTING_STARTED.md)
