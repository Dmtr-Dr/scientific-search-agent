# 🚀 Быстрый старт

## Установка и настройка за 5 минут

### Шаг 1: Подготовка окружения

**Создайте виртуальное окружение:**

```bash
cd ai_agent
python3 -m venv venv
```

**Активируйте виртуальное окружение:**

**macOS/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

### Шаг 2: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### Шаг 3: Настройка API ключей

**Создайте файл `.env` в корне проекта:**

```bash
echo "OPENAI_API_KEY=ваш_ключ_здесь" > .env
```

Или создайте файл вручную:

```
OPENAI_API_KEY=sk-...
```

**Где получить OpenAI API ключ:**
- Перейдите на https://platform.openai.com/api-keys
- Создайте новый ключ
- Скопируйте и вставьте в файл `.env`

**Настройте email для API (рекомендуется):**

Откройте `config.py` и измените:

```python
API_CONTACT = {
    "email": "ваш.email@example.com",  # ← Замените на ваш реальный email
    "user_agent": "AI-Research-Agent/1.0",
}
```

### Шаг 4: Проверка установки

Запустите базовый тест:

```bash
python tests/test_basic.py
```

Вы должны увидеть:
```
✅ Все тесты пройдены! Агент готов к работе.
```

---

## Первый запуск

### Вариант 1: Интерактивный режим

```bash
python main.py
```

Следуйте инструкциям в консоли.

### Вариант 2: Программный вызов

Создайте файл `my_research.py`:

```python
from main import run_research_pipeline

report = run_research_pipeline(
    query="transformer models for natural language processing",
    time_window=5,      # Поиск за последние 5 лет
    max_papers=40       # Максимум 40 статей для анализа
)

print("Отчёт готов!")
```

Запустите:

```bash
python my_research.py
```

### Что происходит внутри?

```
1. QueryBuilder     → Расширяет запрос (синонимы, варианты)
2. Retriever        → Ищет в 5 источниках (150+ статей)
3. Deduper          → Удаляет дубликаты (~80-100 уникальных)
4. Ranker           → Ранжирует по релевантности
5. Summarizer       → Анализирует топ-10 детально
6. GapMiner         → Находит research gaps
7. Ideator          → Генерирует 5 идей для исследований
8. Reporter         → Создаёт Markdown отчёт
```

**Время выполнения:** ~2-3 минуты  
**Стоимость:** ~$0.003-0.01 (OpenAI API)

---

## Примеры запросов

### Machine Learning
```python
"transformer models for natural language processing"
"few-shot learning computer vision"
"reinforcement learning robotics"
```

### Биомедицина
```python
"CRISPR gene editing in cancer treatment"
"immunotherapy melanoma clinical trials"
"alzheimer disease early diagnosis biomarkers"
```

### Физика
```python
"quantum computing algorithms"
"topological insulators materials"
```

### Междисциплинарные
```python
"climate change machine learning predictions"
"AI drug discovery molecular design"
```

---

## Структура отчёта

Каждый отчёт содержит:

✅ **Executive Summary** — краткая сводка  
✅ **Топ-10 статей** — с метаданными, DOI, URL  
✅ **Литературная матрица** — методы, датасеты, метрики  
✅ **Research Gaps** — выявленные лакуны (3-10)  
✅ **Идеи** — проверяемые гипотезы (5)  
✅ **Метрики** — статистика поиска  
✅ **Заключение**

Отчёт сохраняется как `research_report_*.md`

---

## Настройка параметров

Измените в `config.py`:

```python
# Количество результатов с каждого источника
SEARCH_CONFIG["per_source_limit"] = 30

# Веса ранжирования (semantic search - основной компонент)
RANKING_WEIGHTS = {
    "semantic_search": 0.75,    # Основной вес
    "recency": 0.10,            # Вес свежести
    "cpy": 0.08,                # Citations per year
    "ctotal": 0.05,             # Total citations
    "venue": 0.02,              # Престижность venue
}

# Опциональные функции
EXPERIMENTAL = {
    "enable_semantic_search": True,   # Векторный поиск (рекомендуется)
    "enable_llm_rerank": False,       # LLM rerank (опционально)
    "enable_snowballer": False,       # Citation snowballing
    "enable_replanning": False,       # Агентное перепланирование
}
```

---

## Troubleshooting

### ❌ Ошибка: "OPENAI_API_KEY not found"

**Решение:** Создайте файл `.env`:
```bash
OPENAI_API_KEY=sk-...
```

### ❌ Ошибка: "Rate limit exceeded"

**Решение:** Подождите 1 минуту или используйте другой API ключ

### ❌ Слишком мало результатов

**Решение:** 
- Расширьте `time_window` (например, до 10 лет)
- Используйте более общие термины в запросе
- Проверьте доступность API источников

### ❌ Долгое выполнение (>5 мин)

**Решение:**
- Уменьшите `max_papers` (например, до 20)
- Отключите некоторые источники в `config.py`:
  ```python
  SOURCES_ENABLED["pubmed"] = False
  ```

### ❌ Ошибка при установке зависимостей

**Решение:**
```bash
pip install --upgrade pip setuptools wheel
pip install -r requirements.txt
```

---

## Продвинутое использование

### Кастомная конфигурация

```python
from main import run_research_pipeline
from config import RANKING_WEIGHTS

# Изменяем веса на лету
RANKING_WEIGHTS["recency"] = 0.20
RANKING_WEIGHTS["semantic_search"] = 0.65

report = run_research_pipeline("your query")
```

### Пакетная обработка

```python
queries = [
    "transformer models NLP",
    "CRISPR gene editing",
    "quantum computing"
]

for query in queries:
    print(f"Processing: {query}")
    report = run_research_pipeline(query, time_window=3, max_papers=20)
    # Отчёты сохраняются автоматически
```

---

## Деактивация окружения

После завершения работы:

```bash
deactivate
```

---

## Следующие шаги

1. 📖 Прочитайте [README.md](../README.md) для полного описания возможностей
2. 🔑 Настройте все API в [API_SETUP.md](API_SETUP.md)
3. 🏗️ Изучите архитектуру в [ARCHITECTURE.md](ARCHITECTURE.md)
4. 🔬 Экспериментируйте с разными темами!

---

**Happy researching! 🎓✨**
